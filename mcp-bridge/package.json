{
  "name": "vericlaw-mcp-bridge",
  "version": "1.0.0",
  "description": "MCP bridge sidecar for VeriClaw — exposes MCP server tools via REST",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "express": "^4.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
